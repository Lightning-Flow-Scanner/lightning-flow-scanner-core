import { describe, it, expect } from "@jest/globals";

describe("TreeNode ", () => {
  it("should unpack a tree", () => {
    expect(true).toBe(true);
  });

  it("should unpack a tree with recursive elements", () => {
    expect(true).toBe(true);
  });

  it("should unpack a tree with wide elements", () => {
    expect(true).toBe(true);
  });
});
